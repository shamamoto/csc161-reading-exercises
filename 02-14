1. 3?
2. 0
3. main is run first. Then int x is replaced by 3. Which is then changed to 0. That 0 is printed.

The x in change has a value of 0. The assignment changes the previously
defined variable (3) as 0 - thus the output recieved. 
